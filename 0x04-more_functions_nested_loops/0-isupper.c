#include "main.h"

/**
 * _isupper - uppercase character
 * @c: character to be checked
 * Return: 1 if the charachter is uppercase , 0 otherwise 
 */

int _isupper(int c)
{
	if (c >= 65 && ch <= 90)
		return (1);
	else
		return (0);
}
